# MNIST VAE

A simple Variational Autoencoder (VAE) implemented in TensorFlow for MNIST digits.

Features:
- Binary MNIST preprocessing
- Encoder + Decoder with two hidden layers and dropout
- ELBO loss (Binary Cross-Entropy + KL Divergence)
- Latent space visualization
- Reconstruction and random digit generation
